# Frontend env — à configurer dans Vercel → Project Settings → Environment Variables.
# Pas besoin d'un fichier sur disque en prod : Vercel les injecte au build.

NEXT_PUBLIC_API_URL=https://audit-bureau-api.fly.dev
